jQuery(document).ready(function($) {
    if (dpcData.clearFields === 'true') {
        $('#dynamic_pages_creator_title_field').val(''); // Clear the field
        console.log("Fields cleared."); // Optional: for debugging
    }
});
